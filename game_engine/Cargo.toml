[package]
name = "game_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = "0.22"
rand = "0.8"

[lib]
name = "game_engine"
crate-type = ["cdylib"]
